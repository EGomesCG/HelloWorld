using System;

namespace HelloWorld.Aulas
{
    //Enum é uma estrutura resevada onde seus atributos nunca alteram
    public enum Status
    {
        rascunho = 0,
        aberto = 1,
        aprovado = 2,
        fechado = 3,
        cancelado = 4,
    }
    public enum Sexo
    {
        Masculino,
        Feminino
    }
}
